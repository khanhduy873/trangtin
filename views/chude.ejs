<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<title><%= title %></title>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
	<link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('includes/navbar') %>
	<div class="container">
		<div class="card mt-3">
			<div class="card-header"><%= title %></div>
			<div class="card-body table-responsive">
				<a href="/chude/them" class="btn btn-primary mb-2"><i class="bi bi-plus-lg"></i> Thêm mới</a>
				<table class="table table-bordered table-hover table-sm mb-0">
					<thead>
						<tr>
							<th class="text-center" width="5%">#</th>
							<th>Tên chủ đề</th>
							<th width="5%">Sửa</th>
							<th width="5%">Xóa</th>
						</tr>
					</thead>
					<tbody>
						<% var stt = 1; %>
						<% chude.forEach(cd => { %>
							<tr>
								<th class="text-center"><%= stt++ %></th>
								<td><%= cd.TenChuDe %></td>
								<td class="text-center"><a href="/chude/sua/<%= cd._id %>"><i class="bi bi-pencil-square"></i></a></a></td>
								<td class="text-center"><a href="/chude/xoa/<%= cd._id %>" onclick="return confirm('Bạn có muốn xóa chủ đề <%= cd.TenChuDe %> không?');"><i class="bi bi-trash text-danger"></i></a></td>
							</tr>
						<% }); %>
					</tbody>
				</table>
			</div>
		</div>
	</div>
	<%- include('includes/footer') %>
	<%- include('includes/javascript') %>
</body>

</html>